
  # ============================================
  # AIRCRAFT PREDICTION SYSTEM - DEPENDENCIES
  # ============================================

  # === WEB FRAMEWORK ===
  # Flask: Python web framework for building the API and serving the dashboard
  flask==3.0.0

  # Flask-CORS: Enables Cross-Origin Resource Sharing (allows frontend to talk to backend)
  flask-cors==4.0.0


  # === DATA PROCESSING ===
  # Pandas: For working with CSV data and dataframes (like Excel in Python)
  pandas==2.1.3

  # NumPy: Fast numerical computations (arrays, math operations)
  numpy==1.24.3

  # Scikit-learn: Machine learning library (model training, preprocessing, evaluation)
  scikit-learn==1.3.2


  # === MACHINE LEARNING MODELS ===
  # XGBoost: Gradient boosting model (one of the best for tabular data)
  xgboost==2.0.2

  # LightGBM: Another gradient boosting model (fast and accurate)
  lightgbm==4.1.0

  # Imbalanced-learn: Handles imbalanced datasets (when we have more normal flights than failures)
  imbalanced-learn==0.11.0


  # === MODEL PERSISTENCE ===
  # Joblib: Saves trained models to disk so we don't retrain every time
  joblib==1.3.2


  # === MODEL EXPLAINABILITY ===
  # SHAP: Shows which features contributed to each prediction (builds trust)
  shap==0.43.0


  # === VISUALIZATION ===
  # Matplotlib: Creates charts and graphs
  matplotlib==3.8.2

  # Seaborn: Makes matplotlib prettier (better-looking charts)
  seaborn==0.13.0

  # Plotly: Interactive charts for the dashboard
  plotly==5.18.0


  # === UTILITIES ===
  # Python-dateutil: Working with dates and times
  python-dateutil==2.8.2

  # Pytz: Timezone support
  pytz==2023.3


  # === TESTING ===
  # Pytest: Testing framework to verify our code works
  pytest==7.4.3

  # Pytest-Flask: Testing specifically for Flask apps
  pytest-flask==1.3.0


  # === DEVELOPMENT ===
  # Python-dotenv: Load environment variables from .env file
  python-dotenv==1.0.0
  # Gunicorn: Production server for running the Flask app
    gunicorn==21.2.0
    

  #venv\Scripts\Activate to activate virtual environment
  #pip install -r requirements.txt to install dependencies
  # # python backend/api/app.py -to run the API server
  # python backend/models/train_models.py -to train models
  #to train 
  # Navigate to project-cd C:\Users\banee\OneDrive\Desktop\aircraft-prediction-system

  # 1. Generate new data (after editing the file)-python backend/data/generate_incremental_data.py

  # 2. Combine datasets-python combine_datasets.py

  # 3. Preprocess-python backend/data/preprocessing.py

  # 4. Retrain models-python backend/models/train_models.py

  # 5. Start backend-python backend/api/app.py
 